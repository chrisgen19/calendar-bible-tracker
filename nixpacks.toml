[phases.setup]
nixPkgs = ['nodejs_18', 'npm-9_x']
aptPkgs = ['bash']

[phases.install]
cmds = ['npm ci']

[phases.build]
cmds = [
  'npm run migrate:deploy',
  'npm run build'
]

[start]
cmd = 'npm start'