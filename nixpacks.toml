[phases.setup]
nixPkgs = ['nodejs_18', 'npm-9_x', 'bash']
aptPkgs = ['curl', 'wget']

[phases.install]
cmds = ['npm ci']

[phases.build]
cmds = [
  'npm run migrate:deploy',
  'npm run build'
]

[start]
cmd = 'npm start'